<% content_for :title do %>VPNから参考資料フォルダを参照するには？ - こんな時どうする？<% end %>
<% content_for :header do %>VPNから参考資料フォルダを参照するには？<% end %>
<%= render 'header' %>

<p>VPN接続の場合は、<%= link_to "ターミナルサーバー（MetaFrame）", METAFRAME_URL , :target => "_blank" %>のExplorerから以下のフォルダを開いてください。<p>
<p>ターミナルサーバー（MetaFrame）上のExplorerのツールバーより「お気に入り」に追加しておくと、次回からはスムーズに開くことができます。<p>
<span class="enclosed"><%= FILE_SERVER_PATH  %></span>
<p></p>
<p><a href="#" onClick="window.close(); return false;">閉じる</a></p>


